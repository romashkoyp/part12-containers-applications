services:
  app:
    image: todo_app_dev
    build:
      context: .
      dockerfile: dev.Dockerfile
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - 5173:5173
    container_name: todo_app_front_dev
    environment:
      - CHOKIDAR_USEPOLLING=true